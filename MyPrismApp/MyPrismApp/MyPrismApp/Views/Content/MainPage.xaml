<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
			 xmlns:local="clr-namespace:MyPrismApp.Views.Converters"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="MyPrismApp.Views.MainPage"
             Title="MainPage">
	<ContentPage.Resources>
		<ResourceDictionary>
			<local:InvertBooleanConverter x:Key="InvertBooleanConverter"/>
		</ResourceDictionary>
	</ContentPage.Resources>
	<StackLayout HorizontalOptions="Center" VerticalOptions="Center">
		<Label Text="{Binding Title}" />

		<Button Text="Facebook Login" Command="{Binding FacebookLoginCommand}" IsVisible="{Binding IsLoggedIn, Converter={StaticResource InvertBooleanConverter}}"/>
		<Button Text="Logout" Command="{Binding FacebookLogoutCommand}" IsVisible="{Binding IsLoggedIn}"/>

		<Image Source="{Binding FacebookUser.Picture}" IsVisible="{Binding IsLoggedIn}"/>
		<Label Text="{Binding FacebookUser.FirstName}" IsVisible="{Binding IsLoggedIn}"/>

	</StackLayout>
</ContentPage>